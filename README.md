# Bazel Python Hello World

Install [bazelisk](https://github.com/bazelbuild/bazelisk), and then run:
```
bazelisk run //:hello
```

Run the simplified version of Skyframe with:
```
python python/skyframe.py
```
